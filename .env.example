# Backend base URL for server-side calls (Next API routes, middleware)
# Use http in local dev; in production set to https API host
BACKEND_URL=http://localhost:8081

# Optional client-visible alias for debugging or direct calls if ever needed
NEXT_PUBLIC_BACKEND_URL=http://localhost:8081